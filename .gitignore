# Secrets — NEVER commit. A leaked OPS_TOTP_KEY means every totp_secrets.enc
# ever encrypted with it is decryptable by whoever sees the repo.
.env
.env.local
.env.*.local

# Generated NPI parts snapshot (~30 MB) — built from the source xlsx
# at `2. TEMPLATES/Cost Ref data/NPI/NPI Quote part list.xlsx`. Run
# `npm run build:npi-parts` from repo root before `npm run build` so
# Vite copies it into the client bundle. See Sprint S-NPI-PARTS.
client/public/npi-parts/parts-snapshot.json

# v1.3 — Ed25519 license signing keys. Private keys NEVER commit; they
# live OFFLINE in the license admin's vault (~/OpsControl-license-keys).
# Rotation 2026-06-04 retired the old in-repo dev key; tests now sign with
# runtime-ephemeral keypairs, so no private key needs to be tracked.
scripts/license/*-private.pem
desktop/license-pubkey.pem

# v1.3 — autonomous-upgrade artefacts (UPGRADE_LOG.md is checked in;
# raw backups + generated installers are not).
backup/v1.2_pre_upgrade_*.tar.gz
dist/*.dmg
dist/*.exe
_legacy/

# Quarantined secrets from reset-totp / broken decrypts. Forensics-only.
**/totp_secrets.enc.broken-*
**/totp_secrets.enc.corrupt-*

# Runtime DATA_DIR if someone sets it inside the repo by accident.
server/data/
!server/data/.gitkeep

# Build output
client/dist/
client/node_modules/
node_modules/

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/
!.vscode/launch.json
.claude/

# Local logs
*.log
npm-debug.log*

# Runtime artefacts created by seed-admin / authService when run
# outside the desktop sandbox (i.e. directly from the project root).
/users.json
/README_FIRST_LOGIN.txt
client/public/help/*.docx

# Sprint MES-2.8 — Playwright e2e artefacts.
playwright-report/
test-results/
.playwright-tmp/
dist-release/
desktop/dist-electron/
desktop/package.json.backup-*
Data for import/

# UAT raw feedback — contains customer PII, never commit
   docs/uat/runs/*/feedback.md
   docs/uat/runs/*/screenshots/

